1 verify node -v (if nodejs is installed)
2 install the server with the command: npm install -g json-server
3 activate the server with the command json-server, then the json file: json-server db.json
